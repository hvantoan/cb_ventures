import { theme } from "@/config/theme/themeVariables"

const config = {
  topMenu: true,
  menuCollapse: false,
  rtl: false,
  mainTemplate: 'darkMode',
  loggedIn: false,
  theme,
}

export default config
